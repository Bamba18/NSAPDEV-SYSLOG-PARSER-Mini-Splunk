# NSAPDEV-SYSLOG-PARSER-Mini-Splunk
 lightweight, centralized log management and analytics server designed to ingest, parse, and analyze standard syslog files. Paired with a Command Line Interface (CLI) client, the system will handle bulk log file uploads from multiple concurrent client instances. The server will parse the uploaded syslog files into standard components
